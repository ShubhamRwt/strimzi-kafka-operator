apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cruise-control-permissions
  namespace: myproject
subjects:
  - kind: ServiceAccount
    name: my-cluster-cruise-control
    namespace: myproject
roleRef:
  kind: ClusterRole
  name: cruise-control
  apiGroup: rbac.authorization.k8s.io
